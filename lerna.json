{
  "packages": [
    "packages/*"
  ],
  "version": "independent",
  "npmClient": "yarn",
  "useWorkspaces": true,
  "command": {
    "publish": {
      "ignoreChanges": ["*.md", "*.test.*", "**/__tests__/**", ".gitignore", "*.html"],
      "message": "chore(release): publish",
      "push": false
    }
  }
}
